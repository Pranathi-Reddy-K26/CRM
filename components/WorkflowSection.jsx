const steps = [["01","Capture what matters","Bring leads, notes, and conversations into one shared home."],["02","Make your next move","Let a clear view of your pipeline guide the way forward."],["03","Grow together","Turn consistent follow-through into lasting relationships."]];
export default function WorkflowSection() { return <section className="workflow" id="workflow"><div className="section-shell workflow-inner"><div><p className="eyebrow">Clarity at every step</p><h2>Less managing.<br/><em>More doing.</em></h2></div><div className="steps">{steps.map(([number,title,text])=><div key={number}><b>{number}</b><span>{title}</span><p>{text}</p></div>)}</div></div></section>; }
